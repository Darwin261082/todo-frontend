##  `todo-frontend` (Angular Web)

```markdown
# ToDo Frontend Web

Aplicación web desarrollada en **Angular 16+** para gestión de tareas, consumiendo la API `todo-api`.

## Requisitos

- Node.js >= 18
- npm >= 9
- Angular CLI (`npm install -g @angular/cli`)
- API backend corriendo (`http://localhost/todo-api/public`)

## Instalación

1. Clonar el repositorio:

```bash
git clone
